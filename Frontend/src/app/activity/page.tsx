export default function Home() {
  return (
    <main className="flex flex-row min-h-screen">
      <div className="bg-white w-full text-black">Step counter, how many steps a dog has taken each hour, though probably more useful to a user if compiled to a single day of data. Probably best to label this page "step counter" for the user or something rather than activity, which the csv uses</div>
    </main>
  );
}
